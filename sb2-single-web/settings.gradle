pluginManagement {
    repositories {
        maven {
            url uri ("${localRepository}")
            metadataSources {
                mavenPom()
                artifact()
            }
        }

        gradlePluginPortal() //
    }
}

rootProject.name = "${thisProjectName}"
